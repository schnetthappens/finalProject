
<div class="user-show-content-container">
  <div class="user-show-text-container">
    <h2 class="primary-heading user-show-heading">Thanks, {{model.data.user.first_name}}!<br>That was easy.</h2>
    <img class="entry-avatar" src="{{model.data.user.avatar_url}}" alt="" />
    <p class="user-show-text-success">You've been added to the {{model.data.company.name}} team on TimeSweep. By the way, it looks like someone (that's you) entered their time yesterday!</p>
  </div>

  <div class="user-show-latest-entry">
    <h2 class="day" {{action 'toggleList' on='click'}}>Monday</h2>
    <ul class="user-show-entries-list">
      {{#each model.monday.day_entries as |monday|}}
      <li class="entry">
        <div class="entry-wrapper">
          <h2 class="entry-project">{{monday.project}}</h2>
          <h4 class="entry-client">{{monday.client}}</h4>
        </div>
      </li>
      {{/each}}
    </ul>
  </div>

  <div class="user-show-latest-entry">
    <h2 class="day" {{action 'toggleList' on='click'}}>Tuesday</h2>
    <ul class="user-show-entries-list">
      {{#each model.tuesday.day_entries as |tuesday|}}
      <li class="entry">
        <div class="entry-wrapper">
          <h2 class="entry-project">{{tuesday.project}}</h2>
          <h4 class="entry-client">{{tuesday.client}}</h4>
        </div>
      </li>
      {{/each}}
    </ul>
  </div>

  <div class="user-show-latest-entry">
    <h2 class="day" {{action 'toggleList' on='click'}}>Wednesday</h2>
    <ul class="user-show-entries-list">
      {{#each model.wednesday.day_entries as |wednesday|}}
      <li class="entry">
        <div class="entry-wrapper">
          <h2 class="entry-project">{{wednesday.project}}</h2>
          <h4 class="entry-client">{{wednesday.client}}</h4>
        </div>
      </li>
      {{/each}}
    </ul>
  </div>

  <p class="user-show-text-next">For those times when you do forget to enter your time, well, there's just one more step.</p>

  <form class="stripe-form" {{action 'checkout' on='submit'}}>
    <button class="connect-account pure-button pure-button-primary">connect payment</button>
  </form>
</div>



{{yield}}
